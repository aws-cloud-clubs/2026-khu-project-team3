services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
      # 백엔드 API 주소 — 준비되면 주석 해제
      # - BACKEND_URL=http://backend:8080
    restart: unless-stopped
