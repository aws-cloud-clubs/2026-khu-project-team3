[project]
name = "backend-batch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.40.0",
    "kbodata>=0.2.3",
    "playwright>=1.60.0",
    "psycopg2-binary>=2.9.12",
    "pydantic>=2.13.4",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.1",
    "sajupy>=0.2.0",
]

[tool.pytest.ini_options]
markers = [
    "live: runs tests against the real external horoscope endpoint",
]
